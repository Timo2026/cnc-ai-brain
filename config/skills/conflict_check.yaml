name: conflict_check
category: safety
timeout: 30
executor: src.skills.conflict_check
schema:
  type: object
  required: [quote]
  properties:
    quote:
      type: object
      description: 待检查的报价单
