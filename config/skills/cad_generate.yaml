name: cad_generate
category: manufacturing
timeout: 180
executor: src.skills.cad_skill
schema:
  type: object
  required: [part_type]
  properties:
    part_type:
      type: string
      description: 零件类型
    dimensions:
      type: object
      description: 尺寸参数
    output_format:
      type: string
      enum: [step, stl, dxf]
      description: 输出格式
